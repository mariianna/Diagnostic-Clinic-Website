README Only_Index_Modified

This folder contains index as an HTML file which has been modified to reflect the received png image and 
is the only file in the folder which was modified. Instead of creating and modifying classes in the CSS, 
the style of the divs were changed in plain html to give it the look it needed. This file is not connected 
to a database, therefore the information for the posts has been hard-coded.
