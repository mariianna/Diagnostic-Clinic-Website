README CSS_Index_Modified

This folder contains index as an HTML file which has been modified to reflect the received png
image. This file is not connected to a database, therefore the information for the posts has
been hard-coded.

This folder also contains the CSS file which has been modified. No code was deleted from the file,
only added to be used by the index.html.